spring:
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    username: root
    password:
    url: jdbc:mariadb://localhost:41306/test?characterEncoding=utf8
  flyway:
    locations: classpath:/db/migration
  jpa:
    properties:
      hibernate:
        id:
          new_generator_mappings: false